# PR 2627 review resolution

Earlier local follow-up to the onboarding PR, 2026-09-14. Main was merged at `592fa5ed1` in merge commit `43ca68de1`. The user requested a local commit without a push or PR merge. GitHub threads, CI and the Vercel preview therefore still describe the previously published commit.

The problems came from transient component state, array positions used as asynchronous file identities, silently ignored database errors, and automatic transfer to external AI clients. The fixes retain existing import and accounting write paths, remove the development consent bypass and cross-account company lookup, and make external handoffs explicit. A new server-side AI handoff service was considered; a local review/copy dialog achieves the required privacy boundary without adding stored prompts or another API.

Navigation now records questions in browser history and keeps drafts in sessionStorage, scoped to user and company/team, with a two-hour expiry. Company creation and successful exit clear the draft. Browser Back, Forward and refresh restore answers without replaying company creation or bank synchronization. Active writes temporarily disable Back. Before any import has started, selected local files and provider credentials must be reselected after a refresh; they are not serialized into storage. Server import jobs remain authoritative on reload.

The bank picker shows six banks initially, has explicit expand/collapse controls, collapses when search is cleared and bounds the list height. Continue manually stays above the list. The sole-trader VAT-threshold sentence was removed in both locales. New test fixtures use generic names such as Acme AB and Jane Doe.

| Review | Resolution |
| --- | --- |
| Security 4007842754 | Company/task prompts stay in a local review dialog. Chat URLs contain no prompt parameters. Copying and opening the client are explicit actions. |
| Security 4007842765 | Removed the Skatteverket development auto-approve token path. A regression test proves the flag cannot bypass real OAuth. |
| SOC 2 CC8.1 | Removed generated Next.js agent instructions from AGENTS.md. No dependency-generated policy is included. |
| Swedish accounting review | Original voucher descriptions, source series and source numbers remain intact; the journal detail page displays the source reference. The import test verifies descriptions and references across skipped vouchers. No journal write path or existing migration was modified. SIE does not replace retention of original supporting documents. |
| PR-Agent lookup cache | Failed, aborted and not-found lookups are evicted. |
| PR-Agent bank popup | Closing a popup during the pending connect request cancels the attempt instead of navigating the onboarding page. Delayed close callbacks are cleaned up. |
| PR-Agent theater targets | Zero account weights use a uniform fallback. The existing total-zero feed guard remains. |
| 4007979515 | The onboarding layout applies the existing home-domain signpost guard. |
| 4007979536 | Resume-read errors reach the route error boundary instead of opening a new import. |
| 4007979542 | Dashboard connection counts require active Skatteverket tokens. |
| 4007979551 | Removed the service-role cross-account candidate filter; only the user's memberships filter BankID roles. |
| 4007979562 | Popup completion resolves relative URLs against the current origin; executable-script tests cover relative and absolute URLs. |
| 4007979572 | Books exit requires write permission. A viewer receives 403 without clearing the gate cookie. |
| 4007979593 | Findings use the Stockholm calendar date, including summer and winter midnight tests. |
| 4007979598 | A null worklist task returns before rendering the connect footer. |
| 4007979604 | Full-page bank fallback uses location.assign in the event handler. |
| 4007979615 | SIE entries have stable UUIDs. Parsing starts outside state updaters, and late responses only update their own entry. |
| 4007979627 | Both connector entry points use a selectable public address and an explicit open action. Clipboard denial leaves manual copying available. |
| 4007979636 | Bank choices stay hidden until findings are available; failures expose retry and manual continuation. |
| 4007979662 | Cash-flow fallback labels come from the caller's locale. |
| 4007979670 | Back receives extension flags and skips disabled stations. |
| 4007979675 | BankID sole-trader name prefill survives unavailable TIC lookup. |
| 4007979682 | Every direct findings query is checked before computing results; missing-document query errors also propagate. |
| 4007979691 | All three paginated journal-line queries order by unique id. |
| CSS nitpick | Renamed the 23 identified keyframes and references to kebab-case; mixed keyframe selectors use percentages. |

Validation:

- 77 focused test files, 1,351 tests passed, covering onboarding, imports, lookups, OAuth and the reviewed routes.
- Full unit run: 1,776 files passed; 23,759 tests passed. Seven files failed on this Windows checkout: four guard modules could not parse, seven self-host shell cases passed Windows paths to bash, one registration source assertion retained CRLF, and one route-parity assertion used Windows separators. The affected sources/tests are byte-identical in git to origin/main. No test was skipped or weakened to hide these failures.
- Type ratchet passed at the existing 533-error baseline. Full ESLint had zero errors; the final scoped lint also had zero errors. The antipattern guard passed with zero raw error sinks, raw reference fetches or client Node imports.
- Browser verification used the actual components with mock APIs, not a live account: company name/address survived Back, Continue and refresh; bank account selection survived refresh; a 100-bank list stayed bounded; clear-search collapsed to six; canceled popup stayed in onboarding; cash balance used the refreshed value (900 rather than 100); discarded file A's late parse could not overwrite file B; prompt URLs stayed clean; denied clipboard access exposed manual copying. Mobile dialog and form layouts were inspected.

CodeRabbit's advisory docstring coverage score describes the old remote head. It is not a required repository check; this change documents the new navigation and privacy behavior rather than generating repetitive comments across the original 86-file PR. Remote review resolution and new CI/preview results await a push, which was explicitly withheld.

Concurrent dashboard edits were detected during verification. They are preserved in the working tree and excluded from this commit, including their overlapping CTA and translation changes. The local review changes to those shared files were staged separately.

## Final review follow-up, 2026-09-14

The user subsequently authorized publishing the fixes and merging the PR. The separately developed connection-chip changes are now committed and published on the same branch. Main at `870ae5420` was merged in `fdba2c4a7` without conflicts; no feature changes were discarded.

- 4008843570: discard saved pre-import screens when server data confirms books exist. Preserve later bank/Skatteverket drafts and their account selections; replace stale historical import screens with insight while retaining browser indexes.
- 4008843584: open a blank connector popup, detach its opener before navigating, and fall back to the current tab if blocked. Shared by AgentChips and KopplingarChips. Checking the return value of a noopener window directly was rejected because browsers return null even when it opens successfully.
- Outside-diff ProviderStep comment: only require selected years when SIE is available. The register-only path now reaches migration with zero years selected.
- The prior SIE identity, consent, clipboard, active-connection, feature-flag and privacy fixes remain in the published implementation. AiTaskAction replaces the deleted footer; both connection-chip surfaces use AiConnectorDialog for manual address copying.
- The founder's newer quiet manual-bank link below the bounded list and quiet app-exit link are preserved.

Verification on the merged code: 25 focused files and 288 tests passed; the type ratchet passed at 533 existing errors and the antipattern guard passed. Actual-component browser checks with mock APIs confirmed register-only migration starts without fetching SIE, completed-import drafts open insight, bank draft selections survive refresh, and connector popup navigation happens after detaching the opener. Production migration-history reads found 873 recorded versions and no remote-only versions; `20260913120000_api_keys_client.sql` is the only pending repository migration and is already applied on staging. Production migration and deployment status must be verified after the authorized PR merge.
